/**
 * Converts a boolean value to an integer
 * @param {boolean} val - The boolean value to convert
 * @returns {number} The converted value, either 1 or 0
 */

const booleanToInt = (booleanValue) => (booleanValue ? 1 : 0);

export default booleanToInt;
